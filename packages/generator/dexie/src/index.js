const { normalize } = require("./core/normalize.js");
const { generate } = require("./core/generate.js");
const { output } = require("./core/output.js");

module.exports = {
  normalize,
  generate,
  output,
}
